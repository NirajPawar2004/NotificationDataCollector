<?xml version="1.0" encoding="utf-8"?>

<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp">

        <Switch
            android:id="@+id/switchCollection"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Enable Notification Collection"/>

        <Switch
            android:id="@+id/switchAutoExport"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Enable Auto Export"/>

        <Button
            android:id="@+id/btnDailyExport"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Daily Export"/>

        <Button
            android:id="@+id/btnWeeklyExport"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Weekly Export"/>

        <Button
            android:id="@+id/btnMonthlyExport"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Monthly Export"/>

        <Button
            android:id="@+id/btnCancelExport"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Cancel Auto Export"/>

    </LinearLayout>

</ScrollView>